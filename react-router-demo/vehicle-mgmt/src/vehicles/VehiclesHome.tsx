import React from 'react';
import { Container, Typography, TextField } from '@mui/material';

export const VehiclesHome: React.FC = () => {
  return (
    <Container maxWidth="md">
      <Typography variant="h4">Vehicle Home</Typography>
    </Container>
  );
};
